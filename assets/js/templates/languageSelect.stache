<li class="dropdown">
	<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button">{{i18n.selectLanguage}} <span class="caret"></span></a>
	<ul class="dropdown-menu">
		{{#each lang.languages}}
		<li><a href="#" ($click)="lang.switchLang(code)">{{name}}</a></li>
		{{/each}}
	</ul>
</li>